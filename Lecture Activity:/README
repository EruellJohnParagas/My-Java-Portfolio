This Assigment describes the task that i did accomplished during the CVSU clean-up Day, it also answers questions such as:

In the context of the doTask() method, what would be the methods that will be called in order to do a task?, and
Where should the addHours() method be implemented? Why?
